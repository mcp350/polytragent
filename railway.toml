[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn web_server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
